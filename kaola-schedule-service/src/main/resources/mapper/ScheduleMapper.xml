<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kaola.schedule.mapper.ScheduleMapper">

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO t_schedule (masseur_id, store_id, date, start_time, end_time, status, create_time, update_time, deleted)
        VALUES
        <foreach collection="schedules" item="s" separator=",">
            (#{s.masseurId}, #{s.storeId}, #{s.date}, #{s.startTime}, #{s.endTime},
             #{s.status}, NOW(), NOW(), 0)
        </foreach>
    </insert>

</mapper>
