spring:
  application:

  config:
    import: optional:nacos:
    name: kaola-order-service
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: kaola
        group: DEFAULT_GROUP
      config:
        namespace: kaola
        group: DEFAULT_GROUP
        file-extension: yml
